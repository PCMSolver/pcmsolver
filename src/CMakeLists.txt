include_directories(solver)
include_directories(wavcav)
include_directories(wem)
include_directories(pwl)
include_directories(pedra)
include_directories(cavity)
include_directories(metal)
include_directories(green)

add_subdirectory(solver)
add_subdirectory(wavcav)
add_subdirectory(wem)
add_subdirectory(pwl)
add_subdirectory(pedra)
add_subdirectory(cavity)
add_subdirectory(metal)
add_subdirectory(green)

