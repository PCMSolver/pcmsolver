set(list_of_libraries solver green cavity pedra utils)
add_googletest(cpcm_gepol-point "${list_of_libraries}" "${LIBS}")
add_googletest(cpcm_gepol-NH3 "${list_of_libraries}" "${LIBS}")
add_googletest(cpcm_gepol-point_from-file "${list_of_libraries}" "${LIBS}")
add_googletest(cpcm_gepol-NH3_from-file "${list_of_libraries}" "${LIBS}")

if(DEVELOPMENT_CODE)
    set(list_of_libraries solver green cavity tsless pedra utils)
    add_googletest(cpcm_tsless-point "${list_of_libraries}" "${LIBS}")
    add_googletest(cpcm_tsless-NH3 "${list_of_libraries}" "${LIBS}")
endif()
