set(list_of_libraries green)
add_boosttest(green_vacuum "${list_of_libraries}" "${LIBS}")
add_boosttest(green_uniform_dielectric "${list_of_libraries}" "${LIBS}")
add_boosttest(green_ionic_liquid "${list_of_libraries}" "${LIBS}")
add_boosttest(green_anisotropic_liquid "${list_of_libraries}" "${LIBS}")
