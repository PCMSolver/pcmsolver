#!/bin/bash

NOW=$(date +"%d/%m/%Y @ %H:%M")

cd @PROJECT_SOURCE_DIR@/doc/html

git commit -am "$NOW : update Doxygen generated html files." # > /dev/null 2>&1 
git push origin gh-pages # > /dev/null 2>&1 

cd - 
